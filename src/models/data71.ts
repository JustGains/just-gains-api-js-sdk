/**
 * Workout Creator APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, number, object, optional, Schema, string } from '../schema';
import { WeekThumbnail, weekThumbnailSchema } from './weekThumbnail';

export interface Data71 {
  /** Unique identifier for the week. */
  weekId?: number;
  /** Identifier of the user who created or owns this week. */
  userId?: string;
  /** Title or name of the week. */
  weekTitle?: string;
  /** JSON string containing the structured content of the week, including workout plans and notes. */
  weekContent?: string;
  /** Thumbnail image representing the week. */
  weekThumbnail?: WeekThumbnail;
  /** Timestamp of when the week was created. */
  createdAt?: string;
  /** Timestamp of when the week was last updated. */
  updatedAt?: string;
  /** Timestamp of when the week was soft deleted, if applicable. */
  deletedAt?: string;
  /** Identifier of the user who deleted the week, if applicable. */
  deletedBy?: string;
}

export const data71Schema: Schema<Data71> = object({
  weekId: ['weekId', optional(number())],
  userId: ['userId', optional(string())],
  weekTitle: ['weekTitle', optional(string())],
  weekContent: ['weekContent', optional(string())],
  weekThumbnail: ['weekThumbnail', optional(lazy(() => weekThumbnailSchema))],
  createdAt: ['createdAt', optional(string())],
  updatedAt: ['updatedAt', optional(string())],
  deletedAt: ['deletedAt', optional(string())],
  deletedBy: ['deletedBy', optional(string())],
});
