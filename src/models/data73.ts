/**
 * Workout Creator APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { boolean, number, object, optional, Schema, string } from '../schema';

export interface Data73 {
  /** Unique identifier for this program view. */
  programViewId?: number;
  /** Identifier of the viewed program. */
  programId?: number;
  /** Identifier of the user who viewed the program, if authenticated. */
  userId?: string;
  /** Unique identifier for the user's session. */
  sessionId?: string;
  /** Timestamp of when the program was viewed. */
  viewedAt?: string;
  /** Duration of the view in seconds. */
  viewDuration?: number;
  /** IP address of the viewer. */
  ipAddress?: string;
  /** User agent string of the viewer's browser. */
  userAgent?: string;
  /** Indicates whether the viewer was authenticated during this view. */
  isAuthenticated?: boolean;
}

export const data73Schema: Schema<Data73> = object({
  programViewId: ['programViewId', optional(number())],
  programId: ['programId', optional(number())],
  userId: ['userId', optional(string())],
  sessionId: ['sessionId', optional(string())],
  viewedAt: ['viewedAt', optional(string())],
  viewDuration: ['viewDuration', optional(number())],
  ipAddress: ['ipAddress', optional(string())],
  userAgent: ['userAgent', optional(string())],
  isAuthenticated: ['isAuthenticated', optional(boolean())],
});
