/**
 * JustGains-APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { boolean, lazy, object, optional, Schema, string } from '../schema';
import { MediaAsset, mediaAssetSchema } from './mediaAsset';

/** The data returned by the operation. */
export interface Locale1 {
  /** A unique code for the locale. */
  localeCode?: string;
  /** The name of the locale. */
  localeName?: string;
  /** Indicates whether the locale is published. */
  published?: boolean;
  localeIcon?: MediaAsset;
}

export const locale1Schema: Schema<Locale1> = object({
  localeCode: ['localeCode', optional(string())],
  localeName: ['localeName', optional(string())],
  published: ['published', optional(boolean())],
  localeIcon: ['localeIcon', optional(lazy(() => mediaAssetSchema))],
});
