/**
 * Workout Creator APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ExerciseCategoriesController } from '../src';
import { testClient } from './testClient';
import { expectHeadersToMatch, makeApiCall } from './testHelper';

describe('ExerciseCategoriesController', () => {
  let controller : ExerciseCategoriesController;

  beforeAll(() => {
    controller = new ExerciseCategoriesController(testClient);
  });

  it('should Test getExerciseCategories', async () => {
    const localeCode = 'en-US';

    const response = await makeApiCall(
      () => controller.getExerciseCategories(localeCode)
    );

    expect(response.statusCode).toBe(200);

    const expectedHeaders = { 'Content-Type': ['application/json', true] };

    expectHeadersToMatch(response.headers, expectedHeaders);
  });
});
