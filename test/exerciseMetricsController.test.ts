/**
 * Workout Creator APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ExerciseMetricsController } from '../src';
import { testClient } from './testClient';
import { expectHeadersToMatch, makeApiCall } from './testHelper';

describe('ExerciseMetricsController', () => {
  let controller : ExerciseMetricsController;

  beforeAll(() => {
    controller = new ExerciseMetricsController(testClient);
  });

  it('should Test getExerciseMetrics', async () => {
    const localeCode = 'en-US';

    const response = await makeApiCall(
      () => controller.getExerciseMetrics(localeCode)
    );

    expect(response.statusCode).toBe(200);

    const expectedHeaders = { 'Content-Type': ['application/json', true] };

    expectHeadersToMatch(response.headers, expectedHeaders);
  });
});
